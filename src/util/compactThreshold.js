/* eslint-disable import/prefer-default-export */

// Put threshold here if they are shared accross multiple components
// and need or should be the same.

/**
 * When to collapse the base UI
 * and switch to showing the compact UI.
 */
const baseCompactThreshold = 750;

export { baseCompactThreshold };
